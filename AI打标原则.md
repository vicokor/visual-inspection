# AI 识别打标原则与落地技术路线

## 1. 文档定位

本文定义 AI 视检后续优化时必须遵守的识别、比较、忽略、归因和打标规则。`v23.0` 已按本文开始重建 AI 识别链路；页面交互、人工标注、历史数据和导出语义继续保持兼容。

适用前提：

- 设计图是正确参考，开发图是问题承载图，所有自动标注只落在开发图坐标系。
- 核心目标是校对前端视觉样式，不负责校验业务文案、运营配置和一般数据内容是否正确。
- 默认开启现有的“忽略内容差异”和“忽略重复组件数量差异”时，应优先降低误报；关键操作缺失不受这两个开关豁免。
- 仅凭截图不能准确反推 CSS 源码。字号、间距、边框、圆角、阴影等数值均指基准宽度下的视觉测量值。

## 2. 总体结论

后续实现不能继续把“图片 A 修改后得到图片 B”作为主要假设，也不能只依赖固定位置、像素残差或模块顺序进行匹配。正确路线是：

1. 先把两张页面解析为元素和组件。
2. 再按元素类型、语义角色、视觉特征和上下文关系建立对应。
3. 对已对应元素分别比较样式属性。
4. 将内容差异、数量差异、顺序变化和连带位移从样式问题中剥离。
5. 最后按根因合并，只输出高置信、可解释且定位准确的标注。

现有像素差异和局部配准能力继续保留，但角色应从“主要识别依据”调整为“匹配后的局部验证证据”。

## 3. 打标最高原则

### 3.1 样式优先，内容弱化

重点检查以下视觉属性：

- 元素尺寸、比例、裁切和可见范围。
- 字号、字重、行高、字距、文字颜色及对齐。
- 元素之间及元素与容器之间的间距。
- 边框粗细、边框颜色、分割线粗细。
- 圆角、背景、透明度、渐变和阴影。
- 图标的尺寸、颜色、对齐及必要图标是否存在。
- 重叠、溢出、遮挡、层级和异常留白。

普通文案、数值、头像、商品图、列表数据及普通重复项的增减属于可忽略内容。忽略内容不等于忽略承载内容的组件样式。

### 3.2 精确优先于数量

- 宁可少标，也不生成大量不能确认根因的冗余框。
- 不能确认元素对应关系或问题类型时，不生成面向设计师的自动标注。
- 不再使用“具体原因待确认”占用设计师的自动标注列表；此类候选仅保留在内部调试证据中，由设计师人工判断和标注。

### 3.3 一个根因只标一次

- 同一个错误造成的连续位移、多个像素碎片或多个子元素残差，只保留一个根因标注。
- 同一元素同时存在多个样式问题时，优先使用一个完整元素框，并在一条描述中列出能够确认的属性。
- 同一种规律性错误出现在多个同构元素上时，只标一个最清晰的代表，并记录受影响数量；不要逐个重复打框。
- 不同根因不能错误合并。例如颜色错误与间距错误应分别保留，因为它们的框选区域和修复方式不同。

### 3.4 局部问题只圈局部

- 字段中的图标错误只圈图标，不圈整个字段或卡片。
- 分割线错误只圈分割线。
- 间距错误只圈开发图中的实际空白区域。
- 单个圆角错误只圈异常圆角；同一卡片四个圆角出现同一种规律性错误时，也只选择一个代表圆角。
- 只有无法拆分且整个组件均错误时，才圈完整组件。

## 4. 页面内容模型

系统必须先建立层级化页面模型，再执行比对。

```text
页面
└── 场景／页面区域
    └── 模块／卡片／列表
        └── 复合组件
            └── 原子元素
```

### 4.1 原子元素

最小可比较对象包括：

- 文字行或文字字段。
- 图标。
- 图片及图片容器。
- 线条、分割线和边框片段。
- 背景色块、圆角和阴影轮廓。
- 可点击控件的可见底板。

### 4.2 复合组件

复合组件由多个原子元素构成，必须先作为整体识别，再检查内部关系。例如：

- 输入字段 = 标签 + 输入文字／占位文字 + 输入区域 + 分割线／边框 + 可选图标。
- 按钮 = 按钮底板 + 文案 + 可选图标。
- 卡片 = 背景 + 圆角 + 阴影／边框 + 内容区。
- 列表项 = 主信息 + 次信息 + 前后图标／图片 + 分隔结构。

输入字段中标签多字、少字或整个普通字段组缺失，是内容差异；标签到输入区域的间距、字号、分割线粗细等仍是样式差异。

### 4.3 重复组

系统应识别列表、表单、卡片、宫格等重复模板，并记录：

- 组内共有结构。
- 可变内容区域。
- 共有样式约束。
- 实例数量。
- 异常实例与异常属性。

重复组用于完成三件事：忽略普通数量变化、跨顺序匹配同类实例、将规律性错误压缩为一条代表标注。

### 4.4 关系不是元素

间距、对齐、包含、相邻和顺序属于元素之间的关系，不应被当成独立像素块。至少需要维护：

- 父子包含关系。
- 同级相邻关系。
- 水平／垂直对齐关系。
- 元素到容器四边的间距。
- 重复组成员关系。
- 关键操作与所属场景的关系。

## 5. 建议的数据结构

每个视觉节点至少保留以下信息：

| 字段 | 作用 |
| --- | --- |
| `id` | 当前图片内的稳定节点编号 |
| `type` | text、icon、image、divider、input、button、card、list-item 等 |
| `role` | label、value、primary-action、secondary-action、navigation 等语义角色 |
| `bbox` / `mask` | 元素框与可见轮廓 |
| `style` | 颜色、字号、边框、圆角、阴影、尺寸等视觉测量值 |
| `children` | 复合组件的子元素 |
| `relations` | 父子、相邻、对齐、间距等关系 |
| `repeatGroupId` | 所属重复模板 |
| `criticality` | ordinary、important、critical |
| `confidence` | 类型、角色、匹配和属性判断的独立置信度 |

差异候选必须同时记录设计节点、开发节点、问题属性、证据、根因编号以及被抑制的连带差异，便于测试和回溯。

## 6. 识别与匹配技术路线

### 6.1 图像标准化

- 统一到用户选择的基准宽度，所有阈值和描述使用基准 px。
- 先处理长截图拼接边界、系统状态栏、滚动条及可配置动态图片区。
- 不通过整页强行拉伸解决高度差；高度差应由内容组增减和局部模块对应关系解释。

### 6.2 元素解析

解析层应组合使用多类证据，而非单一固定特征：

- OCR／字形分析识别文字行和排版锚点。
- 轮廓、边缘和区域分割识别按钮、输入框、卡片、图标、图片、分割线等。
- 对齐、间距和重复周期推断组件分组。
- 视觉分类模型给出元素类型和语义角色，几何规则负责校验边界及关系。

如果继续保持浏览器离线运行，可将 UI 元素检测、OCR 和特征模型导出为浏览器可执行模型；模型只负责生成候选，最终判断仍由确定性规则和视觉证据完成。

### 6.3 分层匹配

匹配顺序为“场景／模块 → 复合组件 → 原子元素”，每一层都允许开发图顺序不同。

候选匹配分数至少包含：

- 元素类型一致性。
- 语义角色一致性。
- 视觉外观和形状相似度。
- 子元素组成相似度。
- 父容器及相邻元素上下文相似度。
- 局部几何关系相似度。

绝对坐标和列表序号只能作为弱证据，不能作为硬匹配条件。候选生成后使用全局最优分配，避免同一个设计元素被多个开发元素重复匹配。

### 6.4 顺序无关匹配

- 模块或同类组件顺序颠倒时，先根据内部结构和角色寻找对应项，再比较其内部样式。
- 只要模块本身完整且内部样式正确，纯顺序变化不自动标注。
- 若顺序变化同时造成真实的重叠、裁切或异常间距，只标实际视觉后果，不标“顺序错误”。

### 6.5 局部对齐与属性比较

已匹配组件先单独对齐，再比较属性，避免一个模块的移动污染整页：

- 平移和缩放只用于验证原因，不应用来消除真实尺寸错误。
- 文字内容像素可以屏蔽，但文字可见高度、颜色、基线、行高和承载区域仍需比较。
- 圆角通过角部轮廓比较，边框和分割线通过边缘带宽比较，阴影通过组件外缘的扩散范围和明暗分布比较。
- 间距通过已匹配元素边界测量，不通过差异碎片的高度猜测。

## 7. 忽略规则与关键例外

### 7.1 忽略内容差异开启时

以下情况默认不标：

- 标签、标题、说明、数值等文案不同，包括多字、少字和换成其他业务内容。
- 普通头像、商品图、运营图等内容不同，且已开启动态图片忽略。
- 一个普通输入字段、普通列表项或非关键展示模块增删。
- 因上述可忽略内容增删产生的预期页面重排。

以下情况仍需标：

- 现存元素的字号、颜色、边框、圆角、阴影、尺寸或内部间距错误。
- 内容变化造成溢出、遮挡、重叠、裁切、容器破坏或异常留白。
- 关键按钮、关键导航或完成当前任务所必需的控件缺失。
- 图片容器尺寸、比例、圆角或裁切方式错误；即使图片内容本身被忽略也要检查。

### 7.2 忽略重复组件数量差异开启时

- 设计图 4 个普通表单组、开发图 3 个时，不标缺失的第 4 组。
- 设计图和开发图普通卡片、列表项数量不同，不标数量本身。
- 保留下来的实例仍需逐项检查共同样式。
- 数量差异造成的后续整体上移／下移应做虚拟重排后再比，不得把每个后续模块都标为位置错误。
- 若删除普通内容后，固定容器内出现可确认的底部留白不一致，应标实际的“元素底部间距偏大／偏小”，而不是标“字段缺失”或圈整张卡片。

### 7.3 关键元素优先级高于忽略开关

以下元素默认属于关键候选：

- 提交、确认、保存、支付、登录、注册、下一步等主操作按钮。
- 页面中唯一的主要行动按钮。
- 完成当前流程所必需的返回、关闭或导航控件。
- 能改变流程结果且没有等价替代的操作入口。

关键性不能只根据文字判断，应结合按钮形态、视觉层级、页面场景和唯一性。项目后续应允许通过规则配置关键角色，避免模型误判。

关键元素缺失时，即使开启内容和数量忽略也必须标注。缺失元素没有开发图实体框时，应利用已匹配父容器和相邻锚点，将设计位置映射到开发图的预期区域；映射证据不足时交给设计师，不随意圈整块页面。

### 7.4 整体场景缺失

登录区、支付区、结果区等承担完整任务的主场景整体缺失，已经超出普通文案或列表数量变化：

- 能够可靠定位其预期区域时，生成一条“关键场景缺失”的场景级标注。
- 不把场景内部每个缺失子元素分别打标。
- 该情况生产中较少见，设计师通常会直接要求开发重新配置；因此允许人工升级处理。
- 无可靠场景锚点时不自动猜测位置，留给设计师人工标注。

## 8. 特定样式的判断规则

### 8.1 表单

- 将标签、输入内容、输入容器、分割线和可选图标识别为一个复合字段。
- 文案变化或普通字段整组增减按内容／数量规则处理。
- 标签到输入内容、输入内容到分割线、字段到下一字段及最后字段到容器底部的距离分别测量。
- 字段数量变化后先进行虚拟重排，再判断剩余字段的内部样式和容器边距。

### 8.2 圆角、边框和阴影

- 一个卡片的四个圆角属于同一个样式约束；同类错误只圈一个实际异常圆角。
- 仅某一个角异常时，圈该角；多个角呈不同错误时，每种错误各保留一个代表。
- 边框宽度和颜色分开取证，但属于同一边框且框选区域一致时可合并描述。
- 阴影只描述截图可以确认的“缺失、过重、范围或方向不一致”，不伪造 CSS 参数。

### 8.3 文字

- 忽略文案时，不能用字符串长度或文字包围框宽度直接判定字号错误。
- 应比较字形可见高度、笔画粗细、基线、行高、颜色和相对锚点。
- 标签变长后，输入内容的位置应相对字段容器或统一栅格比较，不能简单相对标签末尾比较。
- 文案变长引起的正常换行可忽略；若换行导致遮挡、溢出、截断或破坏容器，则标视觉后果。

### 8.4 图标和按钮

- 按钮文案变化可忽略，但底板、尺寸、圆角、颜色、文字样式和图标仍需检查。
- 按钮中的必要图标缺失属于组件结构／样式不完整，应圈图标预期区域。
- 普通装饰图标内容不同可按动态内容忽略；导航、状态和操作语义图标不能仅因内容忽略而消失。

### 8.5 间距与连带位移

- 间距标注框覆盖开发图中的实际空白区，描述使用“与上方元素间距”“元素底部间距”等明确关系。
- 上游一个间距错误导致后续整体移动时，只标上游间距，不重复标后续所有元素的位置。
- 被忽略内容的增删导致重排时，先消除该重排；只有消除后仍存在的真实间距异常才标。

## 9. 规律性错误与代表标注

当多个错误满足“同一重复模板、同一属性、同一方向、近似差值”时，视为同一规律性问题：

1. 选择边界最清楚、遮挡最少、置信度最高的实例。
2. 只创建一条标注。
3. 描述中说明“同类组件共 N 处”或在证据字段记录数量。
4. 其余实例作为关联证据，不显示重复问题框。

若只有一个实例偏离共同规律，则只圈该异常实例。若同一重复组存在两种不同错误，例如部分圆角偏大、部分边框偏粗，则每种错误各保留一条代表标注。

## 10. 根因归并与抑制顺序

候选差异生成后按以下顺序处理：

1. 识别可忽略的内容和数量变化。
2. 对受影响区域执行虚拟插入／删除与局部重排。
3. 抑制由已解释根因造成的下游位移和像素残差。
4. 对剩余候选进行元素级属性归因。
5. 合并同一元素、同一修复动作的描述。
6. 对重复模板做规律性去重。
7. 删除低置信及定位不可靠的候选。
8. 将最终框映射到开发图坐标。

禁止使用以下结果：

- 一个普通字段数量变化导致整张表单被圈。
- 一个卡片移动导致卡片内每个文字和图标都被重复圈选。
- 一个圆角错误圈整张卡片。
- 无法解释的区域统一输出“元素与设计稿不一致”。
- 低置信问题使用“可能是 A／B／C”交给设计师排除。

## 11. 置信度与人工接管

类型置信度、匹配置信度、属性置信度和定位置信度应分别计算，不能只使用一个总分掩盖短板。

自动标注至少同时满足：

- 元素类型或复合结构可确认。
- 设计与开发对应关系可确认。
- 问题属性有直接视觉证据。
- 开发图上的框选位置可确认。
- 未被内容、数量、动态图片或渲染噪声规则解释。

以下情况直接留给设计师，不自动标：

- 无法确认两图是否处于同一 hover、disabled、pressed、loading 等交互状态。
- 多个候选元素相似，无法稳定确定一对一对应。
- 颜色差可能由截图色彩管理、透明叠加或状态变化造成，且证据不足。
- 缺失元素无法通过父容器或相邻锚点映射到开发图。

系统可以在内部调试数据中保留这些候选，但不能生成可见标注。

## 12. 标注描述规范

描述必须说明可确认的对象、属性和方向，必要时附设计／开发视觉测量值。例如：

- `元素底部间距偏大 8px（设计约 16px，开发约 24px）。`
- `输入字段分割线偏粗（设计约 1px，开发约 2px）。`
- `按钮圆角偏小。`
- `文字字号偏大，且颜色不一致。`
- `关键提交按钮缺失。`
- `同类卡片圆角偏小，共 4 处；此处为代表标注。`

纯截图无法唯一确定透明度或具体阴影参数时，使用“颜色或透明度不一致”“阴影范围不一致”等视觉描述，不声称已取得 CSS 值。

## 13. 已确认的边界验收场景

下表用于后续实现和回归测试。默认开启“忽略内容差异”和“忽略重复组件数量差异”。

| 编号 | 场景 | 预期处理 |
| --- | --- | --- |
| 1 | 设计有 4 个普通输入字段，开发只有 3 个 | 不标缺失字段；继续检查现存 3 个字段的样式 |
| 2 | 普通字段减少后，最后一个元素到固定容器底部的留白明显异常 | 标实际空白区，描述为“元素底部间距偏大／偏小”；不标字段缺失 |
| 3 | 标签多字、少字或替换文案，排版样式与锚点一致 | 忽略内容，不标 |
| 4 | 普通表单组数量增加，但新增组沿用正确样式 | 忽略数量，不标新增内容 |
| 5 | 多个完整卡片模块顺序颠倒，卡片内部样式正确 | 忽略顺序，不标 |
| 6 | 普通重复卡片数量不同，后续模块发生预期重排 | 忽略数量及其连带位移，不标 |
| 7 | 重复组中只有一个实例的字号、颜色或间距异常 | 只圈该异常实例的实际错误元素／间距 |
| 8 | 多个同构实例出现同一种规律性错误 | 只标一个最清晰代表，并记录受影响数量 |
| 9 | 同一卡片四个圆角出现同一种半径错误 | 只圈一个代表圆角，不圈整张卡片 |
| 10 | 同一卡片只有一个圆角与其余三个不一致 | 只圈该异常圆角 |
| 11 | 关键提交／确认按钮缺失，忽略内容已开启 | 仍需标关键元素缺失；可靠映射其预期区域 |
| 12 | 两图可能处于不同交互状态，无法确认是样式错误还是状态差异 | 不自动标，留给设计师人工判断和标注 |
| 13 | 文案差异本身可忽略，但文案变长造成溢出、遮挡、裁切或异常间距 | 忽略文案内容，必须标由其引发的视觉样式问题 |
| 14 | 按钮文案不同但样式正确；或按钮内必要图标缺失 | 文案不标；必要图标缺失时只圈图标区域 |
| 15 | 模块顺序变化且其中一个模块内部阴影、圆角或间距错误 | 顺序不标，只标该模块内部可确认的样式错误 |
| 16 | 承担主要任务的完整场景整体缺失 | 可生成一条场景级缺失标注，不拆成多个子元素；罕见情况允许设计师直接要求开发重新配置 |

## 14. v23.0 代码边界

`v23.0` 按以下职责拆分识别链路：

| 模块职责 | 实际落点 |
| --- | --- |
| 调度、进度、取消、设置兼容和最终输出 | `ai-inspection-engine.js` |
| 基准宽度、局部几何映射和像素证据验证 | `local-registration.js` |
| 文字、图标、图片、线条、按钮、输入框、卡片候选识别 | `visual-semantic-parser.js` |
| 组件层级、重复组和语义角色构建 | `visual-semantic-parser.js` |
| 顺序无关的分层候选与全局一对一匹配 | `semantic-matcher.js` |
| 颜色、间距、边框、圆角、阴影等属性比较 | `style-evaluator.js` |
| 忽略规则、关键性、根因抑制、代表标注和置信门槛 | `annotation-policy.js` |

对外仍输出现有标注数据，并保留 `source: 'ai'`、状态、开发图基准坐标和现有编辑／删除能力。不要把新模型或中间节点写入历史数据主结构；必要的调试证据使用可选扩展字段。

## 15. v23.0 落地状态与后续顺序

1. `v23.0` 已建立视觉节点、组件关系、差异候选和最终标注的数据协议。
2. `v23.0` 已建立覆盖核心边界的确定性规则回归和 Canvas 合成场景回归。
3. `v23.0` 已完成基础元素解析、复合组件分组、顺序无关匹配和重复组识别。
4. `v23.0` 已实现普通内容／数量差异过滤、连带位移抑制和固定容器底部间距归因。
5. `v23.0` 已实现分割线、颜色、边框、圆角、阴影和容器间距的元素级评估。
6. `v23.0` 已实现关键按钮／关键场景缺失、规律性去重和低置信人工接管。
7. 后续用真实生产截图扩充回归集，针对误判样例继续调整分类、匹配和样式阈值，不回退到整图固定位置匹配。
8. 真实图片验收稳定后再替换旧线上测试版本。

## 16. 发布验收标准

- 相同图片自动标注为 0。
- 普通字段 4→3、3→4 及普通卡片数量变化，在忽略开关开启时不产生内容误标。
- 模块顺序变化但内部样式正确时不产生顺序误标。
- 现存元素的字号、间距、分割线、圆角、阴影和颜色问题仍能被独立检出。
- 关键按钮缺失不能被内容或数量忽略规则过滤。
- 同一规律性错误最多显示 1 条代表标注。
- 内容变化引发的溢出、遮挡、裁切和真实异常留白必须检出。
- 低置信和状态不明确候选不生成可见自动标注。
- 所有自动框均位于开发图，且框选实际错误元素、异常圆角或实际空白区域。
- 不修改现有人工标注、显示过滤、历史记录和 PDF 导出语义。

上述标准是 `v23.0` 及后续版本的持续发布门槛；离线测试通过后仍需使用真实生产截图验收，才能替换旧线上测试版本。
