# 视检工具版本更新记录

## 固定运行约束

- 主要运行环境：电脑端 Chrome。
- 运行方式：百分百离线、单机运行，不依赖云端服务或网络接口。
- 大版本使用整数编号，例如 `v21`、`v22`。
- 小范围功能优化和 Bug 修复使用递增小数编号，例如 `v21.1`、`v21.2`。
- 每次发布均需在本文件记录更新内容，并以对应版本号命名项目目录和最终压缩包。
- 产品语义：设计图是正确参考基准，项目中的所有标注都只针对开发图。

## v23.0（离线测试版）

- 按《AI打标原则》重建本地 AI 识别链路：由“整图位置与像素残差优先”升级为“元素解析 → 结构匹配 → 样式验证 → 打标策略”，原局部配准和像素差异降为匹配后的复核证据。
- 新增视觉语义解析器，区分文字、图标、图片、分割线、按钮、输入字段、卡片与普通元素；同时记录父子结构、语义角色、关键性、重复模板及圆角、边框、阴影等视觉特征。
- 新增全局一对一语义匹配器，综合元素类型、角色、子结构、形状和局部几何建立对应；绝对位置只作弱证据，完整模块顺序颠倒但内部样式正确时不再产生位置误标。
- 重构内容和数量差异处理：普通文案、普通字段及重复项增删在对应忽略项开启时被屏蔽，其造成的后续重排不再连锁圈选；固定容器中保留下来的真实异常留白按“元素底部间距”单独标注。
- 新增关键操作例外：主要提交／确认类按钮缺失不受内容和数量忽略影响；包含关键操作的完整场景缺失时只生成一条场景级标注，并通过相邻已匹配锚点映射到开发图。
- 新增元素级样式评估器，独立检查分割线粗细、填充颜色、边框、单个圆角、阴影和容器底部间距；问题框优先落在实际线条、圆角或空白区域。
- 新增打标策略层：低置信及交互状态不明确候选不再生成“待确认”自动标注；同一元素多属性按元素归并，同一重复模板的规律性错误只显示一个代表标注并记录影响数量。
- AI 检测摘要文案改为“已忽略或合并”，同时统计内容、数量、不确定候选、连带差异及重复标注的过滤数量。
- 新增 `tests/ai-inspection-v23-regression.cjs` 与离线 Chrome 回归页 `tests/ai-inspection-v23-browser.html`，覆盖模块换序、普通字段缺失、关键按钮／场景缺失、不确定候选弃权、内容引发视觉问题、规律性去重、分割线和底部间距等核心规则。
- 保持百分百离线运行；人工标注、长截图、历史数据、AI 标注数据格式、显示过滤和 PDF 导出流程不变。本轮仅交付独立 `v23.0` 离线测试包，不覆盖旧版本。

## v22.15（离线测试版）

- 导出报告命名输入框背景色由 `#F5F7F7` 调整为 `#F5F7FA`，宽度由 240px 增加到 280px；原有无边框、悬停加深和动画过渡保持不变。
- 涂鸦橡皮擦默认尺寸设为 12px，圆圈光标直径与工具尺寸按 1:1 显示。
- 修复橡皮擦圆圈后方的乱码字符：自定义光标不再复用图标字体的 `.eraser` 类名，改用组件专属状态类，彻底阻止 Chrome 黑色方块和 Safari“口”形伪元素被注入。
- 本轮保存为站点测试版本并交付离线测试包，未部署，当前线上 v22.8 站点保持不变。

## v22.14（离线测试版）

- 历史记录批量导出接入“导出报告”命名弹窗；所填名称直接用于最终多页 PDF，空值继续按 `视检报告-MMDD.pdf` 自动命名。
- 导出命名输入框背景改为 `#F5F7F7`，移除边框；鼠标悬停与聚焦时背景轻微加深，并添加 0.2 秒颜色过渡。
- 输入框默认提示文字改为 25% 不透明度的黑色，保持提示清晰但不抢占正文层级。
- 弹窗“导出”按钮恢复原有 PDF 图标，并修正图标与文字的水平、垂直对齐。
- 本轮保存为站点测试版本并交付离线测试包，未部署，当前线上 v22.8 站点保持不变。

## v22.13（离线测试版）

- 修复长图滚动时滑杆中间手柄延迟回到可视区域中心的问题；移除 100ms 防抖和位置动画，滚动过程中同步固定在当前可视区域中点。
- 修复橡皮擦圆圈旁出现浏览器原生方块光标的问题；橡皮擦启用或调节尺寸时仅保留随擦除大小变化的黑色描边圆圈。
- 修复保存后鼠标悬停主标注框触发“待保存”的问题；标注框悬停与图片边界悬停均作为纯界面状态，不计入项目数据变更。
- 点击“导出”后新增居中的“导出报告”命名弹窗，输入框为 240×44px，并提供“取消”和“导出”操作；文件名为空时按 `视检报告-MMDD.pdf` 自动命名，用户输入 `.pdf` 时不会重复扩展名。
- 本轮保存为站点测试版本并交付离线测试包，未部署，当前线上 v22.8 站点保持不变。

## v22.12（离线测试版）

- 涂鸦画笔在开发图内使用字体库自带的铅笔光标；按住 Cmd 滚轮调节时移除十字准星，仅显示匹配当前颜色的像素圆点。光标圆点最小显示为 3px，画笔实际尺寸仍可设置到 1px。
- 橡皮擦常态光标改为与尺寸调节状态一致的黑色描边圆圈，圆圈直径继续跟随实际擦除半径变化。
- 将滑杆移入可滚动画布，使其与长截图和灰色虚线框同步滚动；滑杆底边向虚线框内收 1px，避免高分屏抗锯齿造成视觉越界。
- 修复保存后仅悬停图片边界就触发“待保存”的问题；边界深浅与光标悬停属于纯界面状态，不再计入项目数据变更。
- 新增参考标尺：可从开发图上下边缘拖出横向参考线、从左右边缘拖出纵向参考线；横线在左右模式贯穿设计图左侧至开发图右侧，竖线仅保留在开发图内。
- 已创建的参考线可悬停后再次拖动；按住 Cmd 双击参考线可删除单线，按住 Cmd 双击开发图任一边缘可清空全部参考线。主标注或涂鸦标注开启时，参考线保留显示但临时停止交互，避免事件冲突。
- 参考线仅用于当前视检辅助，不写入历史记录，也不进入 PDF 导出；本轮未部署，当前线上 v22.8 站点保持不变。

## v22.11（离线测试版）

- 长截图上传选项的选中图标缩小为 10×10px，并在复选框内按水平、垂直方向精确居中。
- 修复滑杆模式中两图高度不一致时滑杆过短的问题；滑杆现在覆盖设计图与开发图共用的完整图片容器高度，“调整长截图”入口固定在该容器外，不再被滑杆穿过。
- 修复涂鸦标注跨历史记录串图的问题；加载任意历史记录时先清空当前插件画布和待恢复队列，再以该记录的标注完整替换，空标注记录也会正确清空。
- 画笔在 Cmd + 滚轮调节时显示十字准星及与实际像素粗细一致的中心圆点，圆点跟随当前颜色；橡皮擦显示随实际擦除直径变化的固定黑色描边圆圈。
- 图片可标注区域的浅灰虚线在鼠标进入时自动加深；主标注与右侧涂鸦工具只在图片区域内显示对应工具光标，移出后恢复普通箭头，并禁止在区域外起笔或拦截 Cmd + 滚轮。
- 本轮保存为站点测试版本并交付离线测试包，未部署，当前线上 v22.8 站点保持不变。

## v22.10（离线测试版）

- 标注列表整体从页面左侧移到右侧；桌面端同步调整视检区留白，窄屏布局仍保持视检区优先、标注列表位于下方。
- Shift 快捷偏移增加悬停手势提示，按住 Shift 并移入设计图即可看到可拖动状态；垂直偏移范围由 ±200px 扩大到 ±400px，并同步更新滑杆刻度。
- 设计图与开发图的偏移留白由半透明灰底改为与长截图编辑器一致的透明格子背景，左右和滑杆模式统一显示。
- 画笔、文字、橡皮擦及已画出的箭头不再拦截普通滚轮；仅在按住 Cmd 滚动时调整对应粗细或字号，并同步右侧滑杆与数值。文字字号采用滚轮累计阈值，降低 Mac 平滑滚轮造成的跳变。
- 画笔和橡皮擦在 Cmd 调整期间显示随尺寸变化的准星及像素值提示；已画出的箭头支持原位修改粗细。
- 修复左右模式加载长图后绘图层被尺寸监听压缩到可视窗口高度的问题；现在插件画布跟随完整对比画布高度，向下滚动后仍可直接绘制，无需切换模式恢复。
- 图片替换面板上传或粘贴预览后继续保留“设计图”“开发图”标签，不再被预览状态隐藏。
- 本轮保存为站点测试版本并交付离线测试包，未部署，当前线上 v22.8 站点保持不变。

## v22.9（离线测试版）

- “调整长截图”入口移除图标和按钮投影，保留原有位置、颜色与文字层级。
- 调整窗口左下角“重新自动拼接”字号提升为 14px；悬停效果改为灰色背景，并增加 0.2 秒颜色与背景过渡，不再显示边框。
- 拼接缝进入调整状态时，预览区会按当前窗口和相邻截图高度临时增加上下滚动缓冲，并同步补偿滚动位置；位于首图或末图的拼接缝也能保持黄色分割线不动，继续拖动图片向分割线靠拢。
- 点击拼接缝的勾后，以 240ms 缓出动画回收临时滚动缓冲并重新计算滚动区域；系统开启“减少动态效果”时直接完成回收。
- 新增 Shift 快捷偏移：按住 Shift 后在设计图区域上下拖动，可实时调整设计图垂直偏移；松开鼠标完成设置，并同步更新原有垂直偏移滑杆与数值。
- 本轮交付离线测试包，未覆盖当前线上 v22.8 测试站点。

## v22.8（测试版）

- 修复普通单图视检左上角露出半个“调整长截图”按钮的问题；未形成多图长截图时，入口严格不参与布局和显示。
- “隐藏标注”现在会同步清空标注命中区域、终止框选和缩放状态，并关闭原生与插件画布的指针事件；隐藏后不再能盲选或调整标注框。
- 滑杆模式的分割线高度改为开发图实际可视高度，不再延伸到图片下方或穿过“调整长截图”入口。
- 手动拼接的方向提示改为上下图片共同朝黄线聚拢；上图与下图拖动分别采用正确的 Y 轴换算，上图拖动时补偿预览滚动，使黄色分割线保持原位，点击勾后完成该处调整。
- 本版本同时发布离线测试包和可访问测试网址。

## v22.7（离线测试版）

- “调整长截图”入口仅在已勾选长截图、且开发图确实由至少两张截图合成时显示；单张开发图或取消勾选后立即隐藏，并同步收回底部预留空间。
- 调整入口扩展到左右、滑杆、叠加、反相四种视检模式，统一按开发图实际显示边界计算水平中心，修正滑杆、叠加和反相模式下偏右问题。
- 自动拼接将低于相邻截图高度 10% 的可疑匹配视为失败，明确提示失败的拼接段并顺接保留图片；该状态下开始视检或应用手动调整都不触发自动 AI 检测，只允许用户在 AI 面板主动点击检测。
- 多图上传增加正序/倒序重叠评分；当倒序证据明显更强时自动纠正截图顺序。本轮三张实图可从“底部→中部→顶部”恢复为正确顺序，两处重叠分别识别为 1418px、1542px，输出高度 4636px。
- 修复手动拼接中拖动黄线上方图片时视觉方向相反的问题；移除左下角操作提示，将“重新自动拼接”改为左下角无边框文字按钮，图标与文字垂直居中，悬停时显示灰色边框。
- 长截图 AI 检测默认忽略最右侧 4% 滚动条区域，并增加仅用于长截图的 JPEG/PNG 压缩与缩放碎边降噪；本轮提供的正确长图与三张拼接截图回归由 44 条误标降至 0 条。
- 普通单图视检继续沿用原检测阈值和逻辑，不启用长截图专用降噪。
- 本轮仅交付离线测试包，线上版本保持 v22.1，不自动覆盖。

## v22.6（离线测试版）

- 修正长截图合成原理：相邻截图先识别重叠区域，再分别裁掉上图底部与下图顶部的重复像素，最后按裁切后的有效区域顺序绘制；不再使用图片图层堆叠或覆盖，因此输出高度会真实扣除重叠像素。
- 自动特征匹配默认忽略屏幕最右侧 14% 区域，并避开常见顶部固定栏与底部手势区，降低滚动条、悬浮控件和系统 UI 对匹配的干扰；匹配证据不足时保持无裁切顺接，避免误删内容。
- 手动调整弹窗改为逐拼接缝交互：每两张相邻截图之间显示黄色分割线和箭头按钮；点击箭头进入该缝调整，按钮切换为勾，可拖动线上方或下方图片，仅改变 Y 轴裁切位置，再点击勾完成。
- “调整长截图”入口移动到开发图下方居中，并为入口单独预留显示空间，修正图标、文字与按钮位置错位。
- 勾选“长截图拼接”后支持一次复制多张图片并在开发图区域按 Cmd/Ctrl+V 粘贴；浏览器能读取到多个剪贴板图片文件时，按剪贴板顺序自动识别和拼接。
- 长截图历史字段升级为双侧裁切数据，并兼容读取 v22.5 保存的 Y 轴堆叠位置；IndexedDB 名称与版本不变。
- 本轮仅交付离线测试包，线上版本保持 v22.1，不自动覆盖。

## v22.5（离线测试版）

- 在开发图上传区和视检中的图片替换面板增加“长截图拼接”勾选入口；启用后可一次选择或拖入 2–20 张 PNG、JPG、JPEG 或 WebP 手机截图，单张仍沿用 10MB 限制。
- 多图按用户选择顺序纵向合并，并在本机自动识别相邻截图的像素重叠区域；无法可靠识别时使用首尾顺接，图片不会上传网络。
- 长截图合成结果继续写入原有开发图状态，开始视检、AI 自动检测、人工标注、保存和 PDF 导出均沿用原流程。
- 在左右模式的“开发图”标签旁增加“调整长截图”入口；弹窗支持点选分段、上下拖动、输入 Y 坐标以及 1px／10px 微调，仅允许 Y 轴调整，首张截图固定为起点。
- 手动调整后重新生成无损 PNG 开发长图并刷新当前视检；若已生成 AI 标注则按现有换图规则清除并重新检测，人工标注继续保留。
- 长截图分段、原图与 Y 轴位置作为可选历史字段保存，旧历史记录无需迁移；IndexedDB 名称和版本保持不变。
- 超出 Chrome 稳定画布范围的超长图片在本地等比例优化，避免浏览器因画布高度或总像素过大而崩溃。
- 本轮仅交付离线测试包，线上版本保持 v22.1，不自动覆盖。

## v22.4（离线测试版）

- 标注归并从像素碎片提升为元素级结果：同一文字字段、图片、按钮或小型 UI 元素只生成一条标注，避免字号或图片尺寸变化后出现密集问题框。
- 同一元素存在多个样式问题时合并描述，例如“文字大小和颜色或透明度不一致”“元素大小和颜色或透明度不一致”，标注框覆盖完整元素范围。
- 优化中文、数字与标签的行内分组，开启“忽略内容差异”后可忽略标题追加“哦哦哦好”和净值追加“98”，但仍独立检测文字大小、颜色、行高等样式。
- 优化文字可见高度估算，“最近购买了此基金”等字段字号变化按整个字段范围标注一次。
- 优化局部结构归并，连续碎片构成的下划线、按钮内容和配图区域不再逐片报错；元素内容无法对应时统一描述为“元素与设计稿不一致”。
- 间距问题继续只圈选开发图中的实际空白区域；当下方整段内容整体移动时，以相邻上下模块边界计算间距，不重复标注被连带移动的内容。
- 使用本轮提供的 750×1624 设计图与开发图增加实图回归：在内容差异忽略开启时，稳定输出标签颜色、收益数字颜色、购买提示字号、业绩走势下划线结构、下方灰色间距共 5 条结果。
- 本轮仅交付离线测试包，线上版本保持 v22.1，不自动覆盖。

## v22.3（离线测试版）

- 重构“忽略内容差异”的文字处理：标题等成对文字不再用字符串长度或文字包围框宽度判断异常，内容像素单独屏蔽，文字样式继续独立检测。
- 文字样式层保留字号、颜色或透明度、位置检查；多行文字在能够确认同一排版流时增加可见行高比较。字号与行高描述使用截图测得的近似像素值，避免伪装成不可直接取得的 CSS 属性值。
- 加长标题仍可与原标题配对；开发图单独新增、且无法与设计图对应的文字或元素继续按结构问题标注，不随内容差异一起忽略。
- 间距问题的标注框改为覆盖开发图中上下模块之间的实际空白像素范围。例如设计 12px、开发 18px 时，标注框高度为开发图实际的 18px，而不是圈选下方内容模块。
- 本轮继续按《视检标准》的“文字与排版属性”和“留白（间距）错误”执行；线上版本保持 v22.1，不自动覆盖。

## v22.2（离线测试版）

- 将旧版 8px 横条配准替换为局部模块一对一匹配；分别尝试平移与缩放，只有差异明显下降且可见范围足够时才采用变换，并继续检测变换后的残差。
- 间距由相邻已匹配模块的边界直接测量。连续 B/C 卡片同向下移不再各自报位移；仅在间距发生变化的模块上显示一条问题，并写明设计、开发及差值（基准 px）。
- 新增元素大小归因；同一元素存在大小与颜色问题时允许保留不同原因的两条标注。缩放后的边缘重采样噪声不重复作为外观问题上报。
- 五档灵敏度为：宽松 4px／较宽松 3px／标准 2px／较严格 1px／严格 0.5px。色差阈值依次为 48／36／26／18／10（0–255 RGB 通道平均绝对差，并非感知色差 ΔE）。
- 容差按用户选择的基准宽度计算，源图倍率变化不改变基准下的物理含义；面板实时展示档位及阈值。旧三档偏好迁移为新 1／3／5 档，其余开关保留。
- 保留本地原生 OCR 能力探测，不可用时以字形结构配对。文字内容忽略需同时满足相似字形高度与排版锚点；文字颜色仍独立检查。
- 具体描述采用“间距偏大 20px”“元素大小不一致”“多出文字”等；纯截图无法唯一反推出 CSS 透明度，故仅描述“颜色或透明度不一致”。证据不足时标记“具体原因待确认”，不列举所有可能原因。
- 增加完整分析入口的确定性像素回归，覆盖等图、正/负间距、大小与颜色叠加、文案变量、新增内容、裁切、重复数量开关、不同倍率、容差边界与取消。
- 本轮仅交付离线测试包，尚未覆盖线上 v22.1。样例回归不等同于用户实际截图验证；复杂重叠、多列以及无法稳定分割的区域保留待确认差异。

## v22.1（测试版）

- 收紧“忽略内容差异”的判断边界：只有开发图与设计稿在文字位置、字号和行高基本一致时才忽略文案变量，类似 `ChatGPT` 与 `ChatGPTT` 的纯内容差异不再单独圈出多余字符。
- 增加成对文字上下文校验：开发图单方面新增的文字或元素没有设计稿对应结构时继续报错，避免被内容忽略规则误过滤。
- 增加组件间距归因：识别纵向重复卡片之间的空白增减，将后续卡片重新对齐，只圈选发生变化的间距并生成“间距偏大／偏小”的问题描述。
- 重复组件数量过滤改为完整结构判断；单行新增文字或小图标不再被误判为数量差异。
- AI 设置面板改为固定标题栏与固定底部操作区，仅中间设置项滚动。
- 统一检测灵敏度滑杆在 Chrome 与 iPad Safari 上的轨道、滑块尺寸和选中进度样式。
- 移除面板底部“恢复推荐设置”，在“AI 视检设置”标题右侧新增刷新图标，点击恢复默认设置。

## v22.0（测试版）

- 新增本地自动视检：上传设计图与开发图并开始视检后，在浏览器本机完成视觉差异检测、页面结构对齐、变量过滤和模板化问题描述，不上传图片。
- 新增本地 OCR 能力探测；浏览器支持原生文字识别时自动用于文案变量匹配，不支持时回退到文字结构识别。
- 新增重复结构分段对齐，开启“忽略重复组件数量差异”后，减少卡片或列表数量变化造成的整页误报。
- 新增 AI 视检设置入口，位于图片替换入口右侧；支持自动检测、AI 标注显示、低/标准/高灵敏度以及内容、数量、系统 UI、动态图片、细微渲染差异的独立忽略设置。
- AI 标注沿用原标注框、问题描述、状态切换、编辑和删除方式，界面不额外显示来源标识。
- AI 标注来源仅在数据层保留，用于独立显示、清除和重新检测；用户编辑、调整或切换状态后自动转为人工标注，不会被重新检测覆盖。
- 关闭“显示 AI 标注”后，画布、列表与 PDF 导出同步过滤 AI 标注，人工标注不受影响；重新开启后立即恢复。
- 历史记录兼容 AI 标注扩展字段，不升级 IndexedDB 版本，不删除已有历史数据。
- 页面标题改为由 `js/app-version.js` 的发布版本配置自动生成，后续版本只维护一个版本号来源。

## v21.3

- “暂无标注”空状态调整为在标注区域内水平、垂直居中。
- 修正模式切换器切换到“反相”时滑块贴边、越界的问题。
- 滑杆模式下为开发图显示区域补充灰色底层，避免两图高度不一致时透出设计图。
- 恢复 PDF 的白色波点背景，并将波点不透明度由 30% 提升至 45%；PDF 图片压缩参数保持不变。
- “左右”模式的设计图与开发图标签改为内容自适应的全圆角胶囊样式，左右内边距为 12px；开发图标签文案简化为“开发图”。

## v21.2（测试版）

- 新增“左右”视检模式：左侧显示设计图，右侧显示开发图。
- 开始视检后默认进入“左右”模式。
- 在“左右”模式中，问题框选以及画笔、箭头、矩形、文字、橡皮擦均只允许在右侧开发图区域操作，不能在设计图区域创建标注。
- 切换回滑杆、透明度、色差模式后，继续保持原有显示与标注交互。
- 历史记录保存和恢复兼容新增模式；旧记录未包含模式字段时默认使用“左右”模式。
- 修复 iPad/平板宽度下标注列表占据首屏、主画面需要缩小和整页滚动才能查看的问题；竖屏时主视检区优先显示，标注列表收纳到底部横向滚动区。
- 问题框选与画笔、箭头、矩形、文字、橡皮擦改为统一 Pointer Events 交互，支持鼠标、触控板和手指触屏操作。
- 触屏标注同样遵循“所有标注只针对开发图”的规则；左右对比模式下设计图区域仍禁止起笔。
- PDF 导出逻辑、尺寸与压缩参数保持不变。

## v21.1

- 修复完整图片在保存历史记录时被反复转为 JPEG、反复压缩的问题。
- 用户新上传的 PNG、JPG、JPEG、WebP 图片按原始格式和原始编码内容保存。
- 从历史记录重新载入图片后再次保存，直接复用已保存的图片数据，不再进行二次有损编码。
- 历史缩略图仍保持压缩，避免历史列表占用不必要的空间。
- PDF 导出逻辑、尺寸、JPEG 压缩参数和现有文件体积控制策略保持不变。

## v21

- 完成项目代码和样式的模块化拆分。
- 保持原有页面结构、交互流程、历史数据格式与 PDF 导出结果不变。
