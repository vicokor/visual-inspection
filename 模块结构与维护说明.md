# 视检工具模块结构与维护说明

## 重构目标

本次重构只调整代码组织方式，不改变现有页面结构、交互流程、视觉样式、历史数据格式与导出结果。拆分遵循“一个稳定职责对应一个模块”，避免把每个小函数拆成独立文件。

## 当前结构

```text
视检工具_v23.0_测试版/
├── index.html                    # 页面结构与脚本加载顺序
├── AI打标原则.md                 # v23 识别、忽略、归因与验收原则
├── 模块结构与维护说明.md          # 本说明
├── 版本更新记录.md                # 运行约束、版本规则与逐版本更新内容
├── 视检标准.md                    # AI 视检规则分类与判断基线
├── css/
│   ├── style.css                 # 样式统一入口
│   ├── base.css                  # 变量、页面布局、上传区、通用控件
│   ├── inspection.css            # 对比画布、标注列表、弹窗
│   ├── panels.css                # 历史侧栏、提示、浮层、批量导出
│   ├── controls.css              # 顶部控制栏、宽度选择、图片替换
│   ├── ai-inspection.css         # AI 设置入口、配置面板与检测进度
│   ├── long-screenshot.css       # 长截图入口、黄色拼接缝与Y轴裁切交互
│   ├── Toolbar.css               # 智能标注工具栏原有样式
│   └── iconFonts.css             # 图标字体原有样式
├── fonts/
│   └── tabler-icons.woff2
├── tests/
│   ├── ai-inspection-v23-regression.cjs # v23 打标原则确定性回归
│   └── ai-inspection-v23-browser.html # Chrome Canvas 端到端回归页
└── js/
    ├── app-version.js             # 发布版本单一数据源与页面标题
    ├── core/
    │   ├── history-storage.js    # IndexedDB 数据层
    │   └── inspection-tool-core.js # 状态、DOM、公共事件与通用能力
    ├── features/
    │   ├── history-feature.js    # 保存、历史列表、恢复、批量导出
    │   ├── comparison-feature.js # 上传、对比模式、滑杆、缩放、偏移
    │   ├── long-screenshot-feature.js # 多图合成、重叠识别与Y轴手动调整
    │   ├── annotation-feature.js # 问题框选、状态、列表、显隐
    │   ├── ruler-feature.js       # 参考线创建、拖动、删除与标注互斥
    │   ├── export-feature.js     # 当前视检报告 PDF 导出
    │   └── ai-inspection-feature.js # AI 设置、自动分析与标注来源管理
    ├── ai-inspection/
    │   ├── visual-semantic-parser.js # 元素类型、语义角色、组件层级与重复组
    │   ├── semantic-matcher.js # 顺序无关的全局一对一结构匹配
    │   ├── style-evaluator.js # 边框、圆角、阴影、分割线与间距证据
    │   ├── annotation-policy.js # 忽略、关键例外、弃权与代表标注
    │   ├── local-registration.js # 局部映射、文字样式与像素证据
    │   └── ai-inspection-engine.js # OCR、语义流水线与最终输出
    ├── SmartAnnotation/          # 原智能标注插件，保持独立
    ├── exportPDF/                # 原第三方 PDF 依赖，保持独立
    └── script.js                 # 模块组合与应用启动
```

## 模块加载和依赖

`index.html` 按以下顺序加载应用代码：

1. `history-storage.js`：提供历史数据读写能力。
2. `inspection-tool-core.js`：创建共享状态和公共能力。
3. `comparison-feature.js` 提供原图片输入与对比能力，`long-screenshot-feature.js` 在其外层接入多图开发图能力。
4. 原有标注与导出功能加载后，AI 语义解析、结构匹配、样式评估、打标策略、局部配准和调度引擎按依赖顺序加载。
5. `ai-inspection-feature.js`：将 v23 本地分析结果接入现有标注系统。
6. `script.js`：组合模块并在 DOM 就绪后创建应用实例。
7. PDF 库与 SmartAnnotation 插件：沿用原加载位置和初始化时序。

功能模块通过 mixin 组合到同一个 `InspectionTool` 实例，继续共享原有 `state`，因此历史记录格式和插件访问方式保持兼容；入口文件不再承载具体业务实现。

## 后续修改对应位置

| 修改内容 | 首选文件 |
| --- | --- |
| 页面节点、按钮位置、弹窗结构 | `index.html` |
| 全局布局、颜色变量、通用按钮 | `css/base.css` |
| 画布、问题列表、编辑弹窗样式 | `css/inspection.css` |
| 历史侧栏、提示、偏移浮层、批量导出样式 | `css/panels.css` |
| 顶部导航、宽度选择、图片替换面板样式 | `css/controls.css` |
| IndexedDB 字段、增删改查、历史分组 | `js/core/history-storage.js` |
| 全局状态、DOM 缓存、公共事件、快捷键 | `js/core/inspection-tool-core.js` |
| 保存、载入历史、批量历史报告 | `js/features/history-feature.js` |
| 图片上传、对比模式、缩放、偏移 | `js/features/comparison-feature.js` |
| 长截图多图上传/粘贴、重叠裁切、拼接缝调整、分段历史数据 | `js/features/long-screenshot-feature.js` |
| 问题标注、状态切换、列表同步 | `js/features/annotation-feature.js` |
| 参考标尺、参考线拖动与清除 | `js/features/ruler-feature.js` |
| 单次 PDF 报告 | `js/features/export-feature.js` |
| AI 设置、自动检测、显示过滤 | `js/features/ai-inspection-feature.js` |
| 图片标准化、OCR、残差复检与最终结果调度 | `js/ai-inspection/ai-inspection-engine.js` |
| 元素分类、组件层级、关键角色与重复组 | `js/ai-inspection/visual-semantic-parser.js` |
| 模块换序、内容增删下的一对一对应 | `js/ai-inspection/semantic-matcher.js` |
| 分割线、边框、圆角、阴影及容器间距 | `js/ai-inspection/style-evaluator.js` |
| 内容／数量忽略、关键缺失、弃权与去重 | `js/ai-inspection/annotation-policy.js` |
| 局部平移/缩放、文字样式、坐标映射与像素证据 | `js/ai-inspection/local-registration.js` |
| 当前版本号与浏览器标题 | `js/app-version.js` |
| 模块组合或启动顺序 | `js/script.js` |
| 画笔、箭头、矩形、文字、橡皮擦插件 | `js/SmartAnnotation/` |

## 维护约定

- 设计图始终作为正确参考基准；所有问题框选和智能批注在产品语义上都属于开发图。新增对比模式或标注工具时必须保持这一约束。
- 新增功能先判断归属已有功能模块；只有形成独立业务域时才新增 feature 文件。
- `script.js` 只做组合和启动，不在其中继续添加业务逻辑。
- 跨模块共享数据统一放在 `this.state`；临时 DOM 状态保留在实例属性中。
- 历史数据字段调整时，同时检查保存、载入、批量导出以及数据库版本兼容。
- 调整 CSS 时保留 `style.css` 中的导入顺序，避免选择器层叠关系变化。
- AI 标注在界面上与人工标注一致，但数据层必须保留 `source` 字段；显示过滤和重新检测不得依赖文字或序号判断来源。
- 新增标注扩展字段不需要提升 IndexedDB 版本；现有升级回调会重建对象仓库，禁止仅为新增普通字段而升级数据库版本。
- 发布新版本时只更新 `js/app-version.js`，页面标题由该配置自动生成。
- 修改脚本加载顺序前，先确认 `InspectionTool` 必须早于 `Toolbar.js` 的 DOM 初始化回调完成实例化。

## 本次结构变化

- v23.0 新增语义解析、结构匹配、样式评估和打标策略四个 AI 职责模块；`local-registration.js` 不再独自承担元素配对与最终过滤。
- v23.0 的像素残差只在已匹配语义节点内作为复核证据；无法归因的低置信残差保留在内部统计，不生成可见自动标注。
- v23.0 新增确定性规则回归；发布前同时执行脚本语法检查、原则回归及 Canvas 端到端合成场景验证。
- v22.7 未新增模块；拼接失败状态、截图顺序纠正和按钮坐标仍由 `long-screenshot-feature.js` 管理，长截图专用滚动条过滤与渲染降噪分别接入现有 AI 调度和检测引擎。
- v22.6 保持原模块边界，将长截图数据由图层偏移升级为 `topCrops` / `bottomCrops` 双侧裁切；自动合成、手动调整、剪贴板多图和旧历史迁移仍集中在 `long-screenshot-feature.js`。
- v22.5 新增独立的 `long-screenshot-feature.js` 与 `long-screenshot.css`，合成后仍复用 `state.devImage`，未把长截图细节侵入对比、标注、AI 或 PDF 模块。
- 历史记录新增可选 `longScreenshot` 字段；旧记录读取路径、数据库名称和数据库版本不变。

- 将原 6294 行 `js/script.js` 拆为数据层、核心层、四个功能模块和轻量入口。
- 将原 2474 行 `css/style.css` 拆为四个样式模块，并保留统一入口。
- `视检.html` 增加明确的应用模块加载顺序。
- 保留 `SmartAnnotation`、`exportPDF`、字体和图标文件原样，降低回归风险。
- 未修改 DOM 的 id/class、IndexedDB 数据库名称与版本、快捷键、上传限制及 PDF 生成逻辑。

## 版本管理

- 项目主要运行于电脑端 Chrome，并保持百分百离线单机运行。
- 大版本使用整数编号；小范围修复与优化使用递增小数编号。
- 每次发布必须同步更新 `版本更新记录.md`，并使用相同版本号命名项目目录和压缩包。
- 当前离线测试版本为 `v23.0`；旧线上测试站点不在本轮自动覆盖范围内。
- AI 脚本必须按 `visual-semantic-parser.js` → `semantic-matcher.js` → `style-evaluator.js` → `annotation-policy.js` → `local-registration.js` → `ai-inspection-engine.js` 的顺序加载。
- 坐标与几何容差统一按 `分析图宽度 / 基准宽度` 换算，禁止直接使用源图像素作为最终描述的 px。
- `tests/ai-inspection-v23-regression.cjs` 可通过 Node 运行，不依赖浏览器。发布前还需使用完整分析入口验证真实或合成截图，不仅测试某个启发式函数。
