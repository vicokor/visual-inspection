# 视检标准

## 一、几何与尺寸属性（布局异常）

- 尺寸偏差：宽／高像素值不符（拉伸或压扁）。
- 比例失调：宽高比（Aspect Ratio）被破坏，例如 16:9 变为 4:3。
- 裁剪缺失：图片或容器边缘被裁切，内容显示不全。
- 留白（间距）错误：内边距（Padding）或外边距（Margin）偏差超过阈值，例如 2px。

## 二、文字与排版属性（字体异常）

- 字体族错误：系统回退字体（Fallback）导致与设计稿字体不一致。
- 字号偏差：Font-size 偏大或偏小，例如动态缩放失效。
- 行高异常：行距过紧导致重叠，或过松导致视觉断开。
- 字重错误：加粗（Bold）误显示为常规（Regular），反之亦然。
- 字距异常：Letter-spacing 丢失导致文字挤在一起。

## 三、色彩与填充属性（颜色异常）

- 色值错误：Hex／RGB 值与设计稿不符，例如主色偏差。
- 透明度丢失：Opacity 未生效，导致遮罩或毛玻璃效果消失。
- 渐变方向或色标错误：渐变角度反转或色标位置偏移。
- 背景缺失：背景色未填充，呈现透明或白色占位。

## 四、圆角与边框属性（图形异常）

- 圆角缺失：Corner radius 为 0，直角未生效。
- 圆角偏差：圆角半径偏大或偏小，例如 8px 显示为 4px。
- 圆角样式错误：单独角（Top-Left）设置未生效。
- 边框粗细或颜色：Border width 或 color 与设计稿不一致。
- 阴影缺失或过重：Box-shadow 偏移量、模糊值或颜色错误。

## 五、交互与状态属性（动态异常）

- 按压态丢失：Hover／Pressed 状态无反馈，例如颜色或缩放没有变化。
- 加载占位图：图片未加载，显示为破碎图标或默认灰块。
- 内容溢出：文字或元素超出父容器边界（Overflow visible 未隐藏）。
- Z-index 层级混乱：元素被遮挡或浮层无法置顶。

## 六、对齐与栅格属性（位置异常）

- 水平或垂直未对齐：未贴齐栅格基线，存在微小的偏移像素（Sub-pixel）。
- 居中失效：Flex／Grid 居中属性未生效，偏向一侧。

后续新增检测规则时，应明确其所属属性、可由截图直接验证的证据及对应容差。
